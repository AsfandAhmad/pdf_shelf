# pdf_shelf
Tap + Add PDFs → pick several PDFs straight from your phone storage at once Each one opens as its own folder-style tab — switch between them like browser tabs Swipe left/right to flip pages, pinch with the +/− buttons to zoom
